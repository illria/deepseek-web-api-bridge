services:
  deepseek-web-api-bridge-v3:
    build: .
    container_name: deepseek-web-api-bridge-v3
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
